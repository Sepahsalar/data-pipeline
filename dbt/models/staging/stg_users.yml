version: 2

models:
  - name: stg_users
    description: "Cleans and prepares raw user data for downstream models."

    columns:
      - name: id
        description: "Unique user identifier."
        tests:
          - unique
          - not_null

      - name: age
        description: "User age."
        tests:
          - not_null

      - name: age_group
        description: "Categorized age group."
        tests:
          - accepted_values:
              arguments:
                values:
                  - young
                  - adult